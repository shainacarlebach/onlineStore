import { ExistingIdValidatorDirective } from './idvalidator.directive';

describe('ExistingIdValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ExistingIdValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
