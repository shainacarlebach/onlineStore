import { ExistingcodevalidatorDirective } from './existingcodevalidator.directive';

describe('ExistingcodevalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ExistingcodevalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
