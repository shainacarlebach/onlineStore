import { ExistingnamevalidatorDirective } from './existingnamevalidator.directive';

describe('ExistingnamevalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ExistingnamevalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
